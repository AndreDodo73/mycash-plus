/** Oficiais: playbook / Prompt 23 / Project Rules. Sidebar só ≥1280. */
export const BREAKPOINTS = {
  mobileMax: 767,
  tabletMin: 768,
  tabletMax: 1279,
  desktopMin: 1280,
} as const;

/** Larguras do componente Sidebar no Figma (não são containers de página). */
export const SIDEBAR_WIDTH_EXPANDED_PX = 300;
export const SIDEBAR_WIDTH_COLLAPSED_PX = 80;
